import { StyleSheet } from "react-native"

export const commonStyles = StyleSheet.create({
    container: {
      flex: 1,
      backgroundColor: '#fff',
      padding: 20
    },
    mainHeight: {
        height: '90%'
    },
});